server:
  port: 8763
spring:
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8888/
  application:
    name: test-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
